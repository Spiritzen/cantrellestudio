PROMPT CLAUDE CODE — MINI-AUDIT COUCHES HERO / RECTANGLE ORANGÉ

Projet :
D:/CreaSite/CantrelleStudio

OBJECTIF UNIQUE
Diagnostiquer rapidement si le rectangle / carré orangé visible dans le Hero
se trouve AU-DESSUS des boules 3D, ou bien derrière elles.

IMPORTANT
- Ne rien corriger pour l’instant.
- Ne rien refactorer.
- Ne pas lancer un audit global.
- Pas de batterie de tests.
- Pas de git add / commit / push.

On veut seulement une ENQUÊTE CIBLÉE sur les couches visuelles du Hero.

======================================================================
PÉRIMÈTRE DE LECTURE
======================================================================

Inspecter uniquement les fichiers utiles au Hero :

- src/components/hero/HeroScene.tsx
- src/components/hero/HeroSpheres.tsx
- src/pages/index.astro
- src/styles/home.css

Si un autre fichier est réellement impliqué dans le Hero (ex. composant décoratif ou motion),
tu peux le lire aussi, mais uniquement si nécessaire pour expliquer le stacking réel.

======================================================================
CE QU’IL FAUT VÉRIFIER
======================================================================

Identifier précisément :

1. quel élément produit le rectangle / carré orangé visible dans le Hero ;
2. si cet élément est :
   - un background sur un conteneur,
   - un pseudo-élément (::before / ::after),
   - un div décoratif,
   - ou autre ;
3. son positionnement réel (`position`);
4. son `z-index` réel ;
5. le `z-index` du canvas / de la scène 3D ;
6. le `z-index` des filaments si pertinent ;
7. s’il existe un stacking context créé par :
   - `position`,
   - `transform`,
   - `filter`,
   - `opacity`,
   - `isolation`,
   - `will-change`,
   - etc.
8. si, visuellement et techniquement, le rectangle orangé passe :
   - derrière les boules,
   - au-dessus des boules,
   - ou au même niveau dans un ordre DOM particulier.

======================================================================
MESURE / VALIDATION RAPIDE
======================================================================

Dans le navigateur, faire un contrôle simple à 1440px sur la Home :

- repérer le canvas / groupe 3D ;
- repérer l’élément qui porte la zone orangée ;
- vérifier dans l’inspecteur l’ordre réel des couches ;
- confirmer si le rectangle est responsable de l’impression de “voile” sur les boules.

Pas besoin de 1000 captures ni de test mobile.
Un contrôle desktop propre suffit pour ce mini-audit.

======================================================================
RÉPONSE ATTENDUE
======================================================================

Répondre de façon courte et précise avec :

- élément exact responsable de la zone orangée ;
- fichier + sélecteur / composant responsable ;
- position ;
- z-index ;
- couche du canvas 3D ;
- verdict clair :
  - le rectangle est derrière les boules,
  - ou le rectangle est au-dessus des boules,
  - ou cas plus subtil à expliquer ;
- cause exacte du problème si problème il y a ;
- correction minimale recommandée, SANS l’appliquer ;
- STOP.

Aucune modification de code.
Aucune commande git.
