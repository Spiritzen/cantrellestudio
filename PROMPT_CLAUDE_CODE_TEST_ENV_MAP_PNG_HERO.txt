PROMPT CLAUDE CODE — TEST ENVIRONMENT MAP PNG SUR LE HERO 3D

Projet :
D:/CreaSite/CantrelleStudio

Fichier déjà présent :
src/assets/hdr/hdr.png

OBJECTIF UNIQUE
Tester l’image panoramique `hdr.png` comme environment map sur la scène 3D EXISTANTE du Hero,
afin d’observer son influence sur les sphères / artefacts déjà en place.

IMPORTANT
Cette image est un PNG panoramique équirectangulaire, donc ce n’est PAS un vrai fichier HDR
(.hdr / .exr) avec grande plage dynamique.
Elle peut néanmoins être utilisée comme environment map LDR pour produire des réflexions.

Le but de ce sprint est uniquement un TEST VISUEL RÉVERSIBLE.

======================================================================
1. AVANT MODIFICATION
======================================================================

Inspecter l’implémentation actuelle de :
- src/components/hero/HeroScene.tsx
- src/components/hero/HeroSpheres.tsx

Identifier :
- où vit la scène R3F ;
- quels matériaux sont utilisés ;
- s’il existe déjà une environment map / Environment / scene.environment ;
- si les sphères utilisent bien un matériau sensible à l’environnement
  (MeshStandardMaterial, MeshPhysicalMaterial ou équivalent).

NE PAS SUPPOSER.
Le code local actuel fait foi.

======================================================================
2. INTÉGRATION DEMANDÉE
======================================================================

Utiliser :
src/assets/hdr/hdr.png

comme environnement équirectangulaire pour la scène Three.js / React Three Fiber.

Exigences :
- l’image ne doit PAS devenir le background visible du Hero ;
- elle doit seulement influencer les réflexions / l’éclairage environnemental des objets 3D ;
- conserver le fond actuel du site, les filaments, la lueur Ember et toute la DA existante.

Approche attendue :
- utiliser une solution native et propre compatible avec la stack actuelle ;
- soit `Environment` de @react-three/drei si cela accepte proprement le PNG local ;
- soit TextureLoader / useTexture + EquirectangularReflectionMapping ;
- utiliser PMREM si nécessaire pour un rendu PBR correct.

Le choix doit rester simple et adapté à la version actuelle de Three/R3F.

======================================================================
3. PAS DE MODIFICATION DES MATÉRIAUX AU PREMIER TEST
======================================================================

Pour ce premier essai :
- NE PAS retoucher metalness ;
- NE PAS retoucher roughness ;
- NE PAS changer couleur ;
- NE PAS changer transmission ;
- NE PAS changer clearcoat ;
- NE PAS changer les lights existantes ;
- NE PAS changer caméra / positions / animations.

On veut d’abord voir l’effet RÉEL de l’environnement sur les matériaux actuels.

Si les matériaux actuels ne réagissent presque pas à l’environment map,
le signaler dans le rapport mais NE PAS commencer une deuxième version.

======================================================================
4. IMAGE = ENVIRONNEMENT UNIQUEMENT
======================================================================

Très important :

scene.environment = environnement
mais PAS :
scene.background = environnement

Le PNG panoramique ne doit jamais être visible directement derrière le Hero.

L’utilisateur doit seulement percevoir :
- des reflets plus riches ;
- éventuellement une ligne d’horizon ;
- des variations bleu / terre / lumière sur les sphères.

======================================================================
5. ASSET / BUILD ASTRO
======================================================================

Comme le fichier est dans :
src/assets/hdr/hdr.png

l’intégrer de façon compatible avec Astro/Vite.

Ne pas hardcoder un chemin Windows dans le runtime.

Si nécessaire :
- importer l’asset avec l’import Vite/Astro adapté ;
- transmettre son URL finale au loader Three.

Le build GitHub Pages avec base `/cantrellestudio` doit rester compatible.

======================================================================
6. PERFORMANCE / MÉMOIRE
======================================================================

Ce test ne doit pas créer :
- plusieurs chargements de la même texture ;
- une nouvelle texture à chaque render ;
- une fuite mémoire ;
- une regeneration PMREM à chaque frame.

L’environnement doit être chargé une seule fois / mémorisé correctement.

Préserver :
- fluidité de la scène ;
- DPR actuel ;
- frameloop actuel ;
- reduced-motion ;
- comportement mobile actuel.

======================================================================
7. RÉVERSIBILITÉ
======================================================================

La modification doit être facile à retirer.

Idéalement :
- un petit composant / bloc local d’environnement ;
- ou quelques lignes clairement identifiables dans HeroScene.

Ne pas refactorer le Hero entier.

Aucun git add / commit / push.

======================================================================
8. VALIDATION
======================================================================

Après intégration :

- npm run check
- npm run build
- npm run dev

Puis contrôle visuel réel sur :
http://localhost:4321/cantrellestudio

Vérifier :
- Hero toujours visible ;
- sphères toujours présentes ;
- filaments présents ;
- animations intactes ;
- l’image panoramique n’apparaît PAS comme background ;
- les reflets sur les sphères ont changé : OUI/NON ;
- aucune erreur console ;
- aucune baisse évidente de fluidité.

======================================================================
9. RAPPORT COURT
======================================================================

Répondre avec :

- méthode utilisée pour charger l’environnement ;
- fichier(s) modifié(s) ;
- environment map appliquée : OUI/NON ;
- image visible comme background : OUI/NON (attendu NON) ;
- changement visible sur les reflets : OUI/NON ;
- matériaux actuels suffisamment sensibles à l’environnement : OUI/NON ;
- npm run check : GREEN/RED ;
- npm run build : GREEN/RED ;
- fluidité : OK/NON ;
- verdict visuel rapide ;
- STOP.

NE PAS lancer une deuxième itération de matériaux sans validation de Sébastien.
