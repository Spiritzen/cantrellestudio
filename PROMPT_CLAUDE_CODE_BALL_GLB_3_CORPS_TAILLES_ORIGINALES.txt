PROMPT CLAUDE CODE — APPLIQUER BALL.glb AUX 3 CORPS DU HERO EN CONSERVANT LES TAILLES VALIDÉES

Projet :
D:/CreaSite/CantrelleStudio

CONTEXTE
Le test de BALL.glb sur une seule sphère est concluant :
le reflet de l’environment map est nettement plus intéressant que sur les sphères procédurales actuelles.

On veut maintenant utiliser LE MÊME asset BALL.glb pour les 3 corps du système orbital.

IMPORTANT :
IL N’EST PAS NÉCESSAIRE de créer BALL1.glb / BALL2.glb.
On doit charger BALL.glb UNE SEULE FOIS puis le réutiliser proprement pour les 3 instances.

Asset :
src/assets/models/BALL.glb

OBJECTIF UNIQUE
Remplacer les 3 sphères procédurales visuelles par 3 instances du même BALL.glb,
tout en conservant EXACTEMENT la hiérarchie de tailles et les animations orbitales déjà validées :

- SOLEIL = la plus grosse boule ;
- TERRE = plus petite que le Soleil ;
- LUNE = plus petite que la Terre.

Le point essentiel :
REPRENDRE LES TAILLES VISUELLES D’AVANT LE TEST GLB.
Sébastien s’est habitué aux proportions précédentes et veut les conserver.

======================================================================
1. INSPECTION AVANT MODIFICATION
======================================================================

Inspecter le code actuel dans :
- src/components/hero/HeroSpheres.tsx
- éventuellement le composant temporaire créé pour BALL.glb
- HeroScene.tsx si nécessaire

Identifier précisément les anciennes valeurs / proportions utilisées AVANT le test GLB pour :
- Soleil
- Terre
- Lune

Ne pas inventer de nouvelles proportions.

Si les anciennes tailles sont encore présentes dans le code, les réutiliser.
Si elles ont été remplacées pendant le test, retrouver les valeurs dans l’historique local/diff Git si disponible,
sans faire de checkout ni modifier l’historique.

Le but est de préserver le rendu spatial précédent.

======================================================================
2. UN SEUL ASSET, TROIS INSTANCES
======================================================================

Charger BALL.glb UNE SEULE FOIS.

Ne PAS créer :
- BALL1.glb
- BALL2.glb
- trois copies physiques du fichier

Réutiliser proprement la même géométrie / le même matériau ou cloner la scène GLTF si nécessaire.

ATTENTION :
un même Object3D ne peut pas être attaché simultanément à plusieurs parents.
Donc :
- soit réutiliser geometry + material sur trois <mesh> distincts si le GLB est simple ;
- soit utiliser une solution de clone propre compatible R3F/drei (`Clone`, scene.clone(), etc.).

Choisir la solution la plus simple et maintenable selon la structure réelle de BALL.glb.

Le GLB ne doit être téléchargé / parsé qu’une seule fois grâce au cache useGLTF.

======================================================================
3. PROPORTIONS À CONSERVER
======================================================================

Conserver la logique visuelle validée :

1. Soleil
   - plus gros des trois ;
   - conserve sa position et son mouvement actuels ;
   - reprend exactement sa taille visuelle pré-GLB.

2. Terre
   - taille intermédiaire ;
   - plus petite que le Soleil ;
   - conserve son orbite actuelle autour du Soleil ;
   - reprend exactement sa taille visuelle pré-GLB.

3. Lune
   - la plus petite ;
   - plus petite que la Terre ET le Soleil ;
   - conserve son orbite actuelle autour de la Terre ;
   - reprend exactement sa taille visuelle pré-GLB.

IMPORTANT :
Ne pas appliquer le même `scale` brut aux trois BALL.glb.

Les trois utilisent le même modèle source,
mais avec les scales adaptés pour reproduire les anciennes tailles.

======================================================================
4. ANIMATIONS / ORBITES À PRÉSERVER
======================================================================

Ne modifier aucune logique orbitale existante.

Conserver :
- direction des orbites ;
- vitesse ;
- inclinaison ;
- hiérarchie Soleil → Terre → Lune ;
- positionnement au scroll ;
- interaction pointeur ;
- rotation éventuelle ;
- reduced-motion ;
- filaments ;
- trajectoires ;
- composition générale.

On remplace uniquement la FORME VISUELLE des trois sphères.

======================================================================
5. MATÉRIAU / ENVIRONMENT
======================================================================

Conserver :
- le matériau actuel de BALL.glb ;
- l’environment map actuellement branchée ;
- son fonctionnement global via scene.environment.

Ne PAS :
- créer trois matériaux différents ;
- modifier metalness / roughness ;
- modifier l’intensité de l’environnement ;
- modifier les lights ;
- passer au vrai HDRI dans ce sprint.

Les 3 boules doivent refléter exactement le même environnement.

======================================================================
6. PERFORMANCE
======================================================================

Exigences :
- un seul chargement du GLB ;
- géométrie/material réutilisés quand possible ;
- pas de nouvelle boucle rAF ;
- pas de duplication de textures ;
- pas de fuite mémoire ;
- pas de recalcul coûteux à chaque frame.

======================================================================
7. NE RIEN TOUCHER D’AUTRE
======================================================================

Ne pas modifier :
- Hero copy ;
- CTA ;
- lueur Ember ;
- background ;
- filaments ;
- grille de points ;
- Navbar ;
- autres sections ;
- caméra ;
- timings existants.

Aucun git add / commit / push.

======================================================================
8. VALIDATION VISUELLE
======================================================================

Lancer :
- npm run check
- npm run build
- npm run dev

Puis vérifier dans le navigateur :

- 3 BALL.glb visibles : OUI
- Soleil clairement le plus gros : OUI
- Terre plus petite que Soleil : OUI
- Lune plus petite que Terre : OUI
- proportions visuelles identiques à celles d’avant le test GLB : OUI
- orbites inchangées : OUI
- filaments inchangés : OUI
- reflets environment visibles sur les 3 : OUI
- fluidité intacte : OUI

======================================================================
9. RAPPORT COURT
======================================================================

Répondre avec :
- méthode de réutilisation du GLB (geometry/material ou clone) ;
- nombre réel de chargements de BALL.glb ;
- anciennes tailles/scales retrouvées ;
- nouveaux scales appliqués à Soleil / Terre / Lune ;
- hiérarchie de taille préservée : OUI/NON ;
- orbites préservées : OUI/NON ;
- material GLB conservé : OUI/NON ;
- npm run check : GREEN/RED ;
- npm run build : GREEN/RED ;
- fluidité : OK/NON ;
- STOP.

NE PAS créer BALL1.glb / BALL2.glb.
